Hey, create a directory and name it documents and keep all the required docs in the two subdirectories namely student and teacher.
Then create a .env file to keep these values

# Optional: Default model to use

DEFAULT_MODEL=mixtral-8x7b-32768

# Vector database path:
VECTOR_DB_PATH=vector_db

# The folder chroma_db will be created when you run the main.py file for the first time.